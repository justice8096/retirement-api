{
  "name": "retirement-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx --watch src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:shared": "cd shared && npx vitest run",
    "typecheck": "tsc --noEmit",
    "engine:sync": "node tools/sync-engine.mjs",
    "postinstall": "prisma generate --schema=prisma/schema.prisma",
    "db:migrate": "prisma migrate dev",
    "db:migrate:deploy": "prisma migrate deploy",
    "db:seed": "tsx prisma/seed.ts",
    "db:studio": "prisma studio",
    "db:generate": "prisma generate",
    "tailscale:serve": "bash tools/tailscale-serve.sh",
    "tailscale:funnel": "bash tools/tailscale-serve.sh --funnel",
    "tailscale:stop": "bash tools/tailscale-serve.sh --stop",
    "db:backup": "bash tools/backup-db.sh",
    "db:backup:local": "bash tools/backup-db.sh --local-only",
    "db:backup:setup": "powershell -ExecutionPolicy Bypass -File tools/setup-backup-schedule.ps1"
  },
  "imports": {
    "#shared/*": "./shared/*"
  },
  "dependencies": {
    "@clerk/fastify": "^3.1.33",
    "@fastify/cookie": "^11.0.2",
    "@fastify/cors": "^11.2.0",
    "@fastify/helmet": "^13.0.2",
    "@fastify/rate-limit": "^11.0.0",
    "@fastify/swagger": "^9.0.0",
    "@fastify/swagger-ui": "^6.0.0",
    "@prisma/adapter-pg": "^7.7.0",
    "@prisma/client": "^7.7.0",
    "@sentry/node": "^10.57.0",
    "dotenv": "^17.4.2",
    "fastify": "^5.0.0",
    "ioredis": "^5.11.1",
    "stripe": "^22.2.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "prisma": "^7.7.0",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  },
  "overrides": {
    "@hono/node-server": "^1.19.13",
    "hono": "^4.12.21"
  }
}
